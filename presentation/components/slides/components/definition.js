import React from "react";
import {
  Text,
} from "spectacle";

export class ComponentDefinitionSlide extends React.Component {
    render() {
        return (
            <div>
              <Text>
                Components let you split the UI into independent, reusable pieces, and think about each piece in isolation.
              </Text>
            </div>
        );
    }
}
